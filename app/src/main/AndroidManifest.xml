<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginsqlite">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Loginsqlite">
        <activity
            android:name=".Emer"
            android:label="@string/title_activity_emer"
            android:theme="@style/Theme.Loginsqlite.NoActionBar"></activity>
        <activity
            android:name=".TreatmentButton"
            android:label="@string/title_activity_treatment_button"
            android:theme="@style/Theme.Loginsqlite.NoActionBar" />
        <activity
            android:name=".MedicineButton"
            android:label="@string/title_activity_medicine_button"
            android:theme="@style/Theme.Loginsqlite.NoActionBar" />
        <activity
            android:name=".SymptomsButton"
            android:label="@string/title_activity_symptoms_button"
            android:theme="@style/Theme.Loginsqlite.NoActionBar" />
        <activity
            android:name=".DiseaseCabinet"
            android:label="@string/title_activity_disease_cabinet"
            android:theme="@style/Theme.Loginsqlite.NoActionBar" />
        <activity
            android:name=".DiseaseActivity"
            android:label="@string/title_activity_disease"
            android:theme="@style/Theme.Loginsqlite.NoActionBar" />
        <activity
            android:name=".MainPage"
            android:label="@string/title_activity_main_page"
            android:theme="@style/Theme.Loginsqlite.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>